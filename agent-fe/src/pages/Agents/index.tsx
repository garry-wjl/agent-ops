import { PageContainer } from '@ant-design/pro-components';
import { Empty } from 'antd';

function Agents() {
  return (
    <PageContainer header={{ title: 'Agent 管理' }}>
      <Empty description='Agent 管理功能开发中' />
    </PageContainer>
  );
}

export default Agents;
